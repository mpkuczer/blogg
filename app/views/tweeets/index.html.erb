<section class="container mt-4">
  <div class="row justify-content-between">
    <%= render 'profile' %>
    <%= render 'feed', tweeets: @tweeets %>
    <%= render 'who_to_follow' %>
  </div>
</section>
