<div class="mb-3 text-center form-group">
  <%= form_with(model: tweeet) do |form| %>
    <% if tweeet.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(tweeet.errors.count, "error") %> prohibited this tweeet from being saved:</h2>
        <ul>
          <% tweeet.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div>
      <%= form.label :tweeet, style: "display: none" %>
      <%= form.text_area :tweeet, class: "form-control mx-2 my-2", placeholder: "Write a post..." %>
    </div>
      <div>
      <%= form.submit "Create", class: "btn btn-light border" %>
    </div>
  <% end %>
</div>


<%# Can't add class: "btn btn-primary" to form.submit because it generates a <submit> element %>
