<%# In order to render _form, I need to pass the instance variable @tweets.
 This view is dealt with via posts#index, which doesn't set the variable
 Is it a good idea to set @post = Post.new in the controller? %>

<section class="container mt-4">
  <div class="row">
    <%= render 'form', post: @post, btn_text: "Create" %>
  </div>
  <div class="row justify-content-between">
    <div class="col-lg-3">
      <%= render 'profile' %>
    </div>
    <div class="col-lg-6">
      <% @posts.each do |post| %>
        <%= render 'post', post: post %>
      <% end %>
    </div>
    <div class="col-lg-3">
      <%= render 'explore' %>
    </div>
  </div>
</section>

    <%# <div class="col-lg-6">
      <%= render @posts, post: @post %> <%# This is so cool %>
    <%#</div> %>
